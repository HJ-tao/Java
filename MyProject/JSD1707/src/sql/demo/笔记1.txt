事务控制语言(TCL):只控制DML
   1)COMMIT：提交,确认已经进行的数据改变
   2)ROLLBACK:回滚,取消已经进行的数据改变
   3)SAVEPOINT:保存点,使当前的事务可以回退到指定的保存点,便于取消
     部分改变.
1)SQL语句不区分大写:为了增加可读性,建议关键字全大写,非关键字全小写.
2)SELECT sysdate FROM dual   日期
  ‘;’表示一句话的结束,用来区分多句话.执行一句话可以选中执行.
   点击播放按钮执行
3)--表示单行注释  /**/多行注释
4)数据库中所有数据类型默认值都为null

数据类型:
1)NUMBER:数字类型.
  经常是NUMBER(P,S)形式   P---数字的总位数   S---小数点后面的位数
  不写S即为P位整数
2)CHAR:表示固定长度的字符类型
  经常是CHAR(N)形式   N----占用的字节数(最大长度是2000)
3)VARCHAR2只支持Oracle(VARCHAR支持SQL):表示变长的字符类型
  VARCHAR2(N)   N---最多可占用的字节数(最大长度是4000)
4)DATE:用于定义日期时间的数据
  长度是7个字节   默认格式:DD-MON-RR  
  
SQL语句根据功能有不同分类
DDL语句(数据定义语句)
用于操作数据库对象.数据库对象包括:表,视图,索引,序列
1)创建表
   CREATE TABLE 表名(字段名 类型,字段名 类型,...);
   1.1)在数据库中,所有的数据类型默认值都是NULL.但是可以通过使用DEFAULT关键字为指定字段单独设置默认值.在数据库
       中,字符串的字面量是使用单引号括起来的.SQL语句虽然不区分大小写,但是字符串内容是区分大小写的.这一点需要注
       意.例如:gender CHAR(1) DEFAULT ‘M’,salary NUMBER(6,2) DEFAULT 3000
   1.2)NOT NULL约束.要求指定字段在任何情况下不允许为空.和DEFAULT不能一起使用.如: name VARCHAR2(20) NOT NULL
   
2)查看表结构
   DESC 表名
3)删除表
   DROP TABLE 表名
4)修改表
  修改表可以修改表的名字和表的结构
  4.1)修改表名:RENAME old_name TO new_name
  4.2)修改表结构:添加新字段,删除现有字段,修改现有字段
      a)添加新字段  
        ALTER TABLE 表名
        ADD(字段名 字段类型,字段名 字段类型...)
      b)删除现有字段
        ALTER TABLE 表名
        DROP(字段名)
      c)修改现有字段
        修改字段可以修改字段的类型,长度,添加默认值或者约束条件.但是修改字段尽量在表中没有数据的情况下进行,
        否则尽量不要修改字段类型,改长度也尽量只增不减.否则肯能修改失败.
        ALTER TABLE 表名
        MODIFY(原字段名 新字段名(新字段类型))
        例如:job VARCHAR(40) NOT NULL  
             job VARCHAR2(40) NULL

DXL语句(数据操作语言)
DXL用于对表中数据进行增,删,改操作
1.INSERT，插入数据